// Re-export AppNavigator as the main navigation component
// This simplifies the navigation structure and prevents duplicate navigation configurations
import AppNavigator from './AppNavigator';

export default AppNavigator;
